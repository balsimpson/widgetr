export function createAssistantPrompt(widgetrUrl: string): string {
  return `Open Widgetr in the in-app browser at ${widgetrUrl}. Wait until its page actions are ready, then use its getting-started action and open the new-project flow. Wait for me to choose a starter in Widgetr. If I choose Weather, ask for my location in this chat, then connect a public JSON weather source and update the widget from its returned fields. If I choose Bitcoin, use the live BTC / USD data already loaded in Widgetr and help me shape the widget or change its source if I ask. If I add a reference image, read Widgetr's reference handoff and ask one focused question at a time before changing the widget. Start by asking what the widget should help me see at a glance. For a screenshot or UI reference, ask whether to preserve the layout, visual style, or both. For a photo or illustration, ask whether it should be the main content, a background, or an accent. For a chart or data image, ask which information should be live and where it comes from. Do not claim to have analyzed visual details unless the reference is visible to you.`
}
